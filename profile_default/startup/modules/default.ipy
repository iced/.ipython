IN_JUPYTER = get_ipython().config.get("IPKernelApp") is not None

import numpy as np
import pandas as pd

if IN_JUPYTER:
  from tqdm import tqdm_notebook as tqdm
  from tqdm import tnrange as trange
else:
  from tqdm import tqdm
  from tqdm import trange
